<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardViewIng"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:backgroundTint="@color/DarkRed"
        app:cardCornerRadius="45dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toTopOf="@+id/dishTitleIng"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/foodImageIng"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:src="@drawable/logo_icon_white" />


    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/dishTitleIng"
        style="@style/Text3"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center"
        android:ellipsize="none"
        android:maxLines="2"
        android:gravity="center"
        android:text="Chicken"
        android:textAlignment="gravity"
        android:textColor="@color/BlackishGrey"
        app:layout_constraintEnd_toEndOf="@+id/cardViewIng"
        app:layout_constraintStart_toStartOf="@+id/cardViewIng"
        app:layout_constraintTop_toBottomOf="@+id/cardViewIng" />
</androidx.constraintlayout.widget.ConstraintLayout>
