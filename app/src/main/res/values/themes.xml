<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FragmentsBonus" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/DarkRed</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/DarkRed</item>

        <item name="colorSecondary">@color/BlackishGrey</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/BlackishGrey</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/DarkRed</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>

    </style>
    
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/DarkRed</item>
    </style>

    <style name="CenteredTitleTheme" parent="Theme.MaterialComponents.Light">
        <item name="actionBarStyle">@style/Widget.AppCompat.Light.ActionBar.Centered</item>
    </style>

    <style name="Widget.AppCompat.Light.ActionBar.Centered" parent="Widget.AppCompat.Light.ActionBar">
        <item name="displayOptions">useLogo|showHome|showTitle</item>
        <item name="android:gravity">center</item>
        <item name="titleTextStyle">@style/ActionBarTitle.Center</item>
    </style>

    <style name="ActionBarTitle.Center" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:gravity">center</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="Theme.FragmentsBonus" parent="Base.Theme.FragmentsBonus" />
</resources>